import React from 'react';

const TextField = () => (
    <div className="field">
        <div className="control">
            <textarea className="textarea" placeholder="Paste here ABI template" />
        </div>
    </div>
);

export default TextField;